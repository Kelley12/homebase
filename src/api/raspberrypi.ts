import express from "express";
// import { basicStatus } from "./helpers";

export function raspberryPiRouter(): express.Router {
    const router = express.Router();

    //router.post("/", (req, res) => basicStatus(res, ));

    return router;
}
